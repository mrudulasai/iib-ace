name: IIB-ACE
 
on:
  push:
    branches: [ "main" ]
 
  workflow_dispatch:
 
jobs:
  build:
    runs-on: ubuntu-latest
 
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Install curl
        run: sudo apt-get install curl

      - name: Test connection to local server
        run: |
          curl 119.63.131.226

      - name: Run shell script
        run: scripts/rempte_backup.sh
       # run: /tmp/backup/scripts/remote_backup.sh
